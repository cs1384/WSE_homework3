^^^ Please add new info in above ^^^^

restore the original working directory by:
$ cd <where you store this directory>
$ mkdir index
$ mkdir conf
$ cp engine_ori.conf conf/engine.conf
$ cd data
$ ln -s <path to your local wiki folder>

